<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper04">

    <!-- 1. 고정된 INSERT문 (age, grade, regDate가 NULL이어도 NULL로 입력됨) -->
    <insert id="addUser01" parameterType="user">
        INSERT INTO users (
            user_id, user_name, password, age, grade, reg_date
        ) VALUES (
            #{userId,jdbcType=VARCHAR},
            #{userName,jdbcType=VARCHAR},
            #{password,jdbcType=VARCHAR},
            #{age, jdbcType=INTEGER},
            #{grade, jdbcType=NUMERIC},
            #{regDate, jdbcType=TIMESTAMP}
        )
    </insert>
    
        <!-- 2. 고정된 INSERT문 : regDate 만 SYSDATE로 명시 -->
    <insert id="addUser02" parameterType="user">
        INSERT INTO users (
            user_id, user_name, password, age, grade, reg_date
        ) VALUES (
            #{userId},
            #{userName},
            #{password},
            #{age:INTEGER},
            #{grade:NUMERIC},
            SYSDATE
        )
    </insert>

    <!-- 3. 동적 SQL 사용 (age가 null이면 컬럼 제외) -->
    <insert id="addUser03" parameterType="user">
        INSERT INTO users (
            user_id, user_name, password
            <if test="age != null">, age</if>
            <if test="grade != null">, grade</if>
            , reg_date
        ) VALUES (
            #{userId},
            #{userName},
            #{password}
            <if test="age != null">, #{age}</if>
            <if test="grade != null">, #{grade}</if>
            , SYSDATE
        )
    </insert>

    <!-- 4. 동적 SQL + jdbcType 명시 -->
    <insert id="addUser04" parameterType="user">
        INSERT INTO users (
            user_id, user_name, password
            <if test="age != null">, age</if>
            <if test="grade != null">, grade</if>
            , reg_date
        ) VALUES (
            #{userId, jdbcType=VARCHAR},
            #{userName, jdbcType=VARCHAR},
            #{password, jdbcType=VARCHAR}
            <if test="age != null">, #{age, jdbcType=INTEGER}</if>
            <if test="grade != null">, #{grade, jdbcType=INTEGER}</if>
            , SYSDATE
        )
    </insert>

</mapper>
